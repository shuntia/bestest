# Java "massive" submissions

Use this directory to stage many student archives when benchmarking the tester.
The paired configuration feeds two dozen small I/O pairs, which is enough to
stress the progress reporting without requiring heavyweight programs.
